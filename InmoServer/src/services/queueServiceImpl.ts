import { QueueService } from '../interfaces/services/queueService';

export class QueueServiceImpl implements QueueService {
  addJobToQueue(data: JSON): void {
    // Implementar la lógica para añadir trabajos a la cola
  }
}