// require('dotenv').config();

export const validateValuesRange = (input: JSON): JSON => {
  // to do implement

  return input;
};

// from servicetype associated to the sensor
export const checkPriority = (input: JSON): JSON => {
  // to do implement

  return input;
};

export const validateAlertRange = (input: JSON): JSON => {
  // to do implement

  return input;
};

export const notify = (input: JSON): JSON => {
  // to do implement

  return input;
};
